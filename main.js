// Function to append text using += operator
function addGreeting() {
    // Get the HTML element with the id "message"
    let element = document.getElementById("message");

    // Append additional text using += operator
    element.innerText += " Welcome to JavaScript!";
}
