// DOM Document Object Example
document.addEventListener("DOMContentLoaded", function () {
    // Using document.getElementById and innerHTML
    document.getElementById("demo").innerHTML = "The content has been changed using DOM!";
});
